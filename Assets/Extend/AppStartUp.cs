using Extend.Common;
using UnityEngine;

namespace Extend {
	public class AppStartUp : MonoBehaviour {
		private void Start() {
			// VersionService service = new VersionService();
			// service.Initialize();
			// service.StartLua();
		}
	}
}